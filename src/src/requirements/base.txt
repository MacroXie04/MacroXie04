# Django Backend Dependencies — base (shared across all environments)

# Core Framework
Django>=5.2.15,<7.0
djangorestframework==3.17.1
djangorestframework-simplejwt>=5.5.1

# Environment Variables
python-dotenv>=1.2.2

# Admin Theme
django-unfold>=0.91.0,<0.92  # 0.92+ requires Python >=3.12; project targets 3.11

# Rich Text Editor
django-ckeditor-5>=0.2.20
Pillow>=12.3.0
pdf417gen>=0.8.1

# Excel Import/Export
openpyxl>=3.1.5

# Google APIs
google-api-python-client>=2.198.0
google-auth>=2.55.1
gspread>=6.2.1

# AI Runtime
openai-agents[litellm]>=0.7.0,<1.0
litellm>=1.83.13,<2.0

# Email
imap-tools>=1.13.0

# Cryptography (RSA key generation) — floor raised from 41.x to 43.x to pull
# in CVE fixes to the Rust bindings.
cryptography>=49.0.0

# HTML Parsing
beautifulsoup4>=4.15.0

# HTML Sanitization
bleach>=6.4.0

# CORS (needed in every environment so the API is reachable from the SPA)
django-cors-headers>=4.9.0
