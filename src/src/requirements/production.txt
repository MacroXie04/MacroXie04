# Production-only dependencies (WSGI/ASGI servers, PostgreSQL, Redis, S3).
-r base.txt

# Production servers
gunicorn>=26.0.0
uvicorn[standard]>=0.49.0,<1.0

# PostgreSQL adapter
psycopg2-binary>=2.9.12

# Redis cache backend
django-redis>=7.0,<8.0  # 7.x needs py>=3.10 + Django>=5.2 (the old "6.0+ needs py3.12" note was wrong); smoke-tested on py3.11 + redis-py 8

# Storage Backend (S3/R2/OSS compatible)
django-storages[s3]>=1.14.6
boto3>=1.43.40
